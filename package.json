{
  "scripts": {
    "test": "node test.js",
    "clean": "rm -rf *tgz; rm -rf packages/*"
  },
  "dependencies": {
    "jsdom": "^9.0.0",
    "json5": "^0.5.1",
    "request": "^2.83.0",
    "tar": "^4.0.1"
  },
  "devDependencies": {
    "get-folder-size": "~1.0.0",
    "run-series": "^1.1.4",
    "tape": "^4.6.3"
  }
}
